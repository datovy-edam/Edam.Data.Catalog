<UserControl
    x:Class="Edam.UI.Catalog.Controls.CatalogExplorerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Edam.UI.Catalog.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:cat="using:Edam.Data.CatalogModel"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

  <Grid>
    <muxc:TreeView ItemsSource="{x:Bind ViewModel.DataSource,Mode=TwoWay}"
                 Background="Beige">
      <muxc:TreeView.ItemTemplate>
        <DataTemplate x:DataType="local:Item">
          <muxc:TreeViewItem ItemsSource="{x:Bind Children}"
                           Content="{x:Bind Name}"/>
        </DataTemplate>
      </muxc:TreeView.ItemTemplate>
    </muxc:TreeView>
  </Grid>
</UserControl>
